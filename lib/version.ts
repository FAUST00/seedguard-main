/** Single source of truth for the user-facing app version. Update here only;
 * the sidebar footer and Settings → About both read from this so they can
 * never drift apart again. */
export const APP_VERSION = '3.2.0';
